import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  mapContainer: {
    padding: 20,
    paddingTop: 10,
  },
  mapView: {
    overflow: 'hidden',
    borderRadius: 10,
    width: '100%',
    marginBottom: 10,
  },
})
