import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
  button: {
    paddingVertical: 7,
  },
  commentHeader: {
    flexDirection: 'row',
    alignItems: 'center',
  },
  buttons: {
    paddingTop: 20,
    paddingHorizontal: 20,
  },
})
