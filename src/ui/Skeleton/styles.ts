import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  container: {
    paddingHorizontal: 20,
    marginBottom: 30,
  },
  skeleton: {
    height: 210,
    borderRadius: 20,
    overflow: 'hidden',
  },
})
